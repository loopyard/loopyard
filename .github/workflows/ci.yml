name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  MIX_ENV: test
  LOOPYARD_HOME: ${{ github.workspace }}/.loopyard_home

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Cache deps
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Install asset packages (app.css imports from assets/node_modules)
        run: npm ci --prefix assets

      - name: Build assets (showcase scene tests render against the compiled CSS)
        run: mix tailwind loopyard

      - name: Run tests with coverage
        env:
          LOOPYARD_LONG_TIMEOUTS: "1"
          MIX_ENV: test
        run: mix coveralls --exclude docker --exclude macos --exclude ssh --exclude terminal --exclude recovery --exclude slow
        timeout-minutes: 10

  quality:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Cache deps + PLT
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
            priv/plts
          key: ${{ runner.os }}-quality-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-quality-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          mix compile

      # --- Hard gates (block merge) ---

      - name: Secret scan
        run: .github/scripts/secret-scan.sh tree

      - name: Check formatting
        run: mix format --check-formatted

      - name: Module cycle check
        run: mix xref graph --format cycles --fail-above 8

      - name: Dependency audit
        run: mix deps.audit

      # These three used to be `continue-on-error` — reported, never enforced,
      # so 120 credo issues, 6 sobelow findings and 82 dialyzer warnings sat
      # green for months. Each is now a hard gate against a clean baseline
      # (Sept 2026). Structure-only credo checks (complexity, nesting, arity,
      # TODO/FIXME) are report-only via exit_status: 0 in .credo.exs; sobelow's
      # accepted findings and threshold live in .sobelow-conf; dialyzer's
      # known-benign warnings live in .dialyzer_ignore.exs — every skip has
      # a reason next to it.

      - name: Credo
        run: mix credo --strict

      - name: Sobelow (security)
        run: mix sobelow --config

      - name: Dialyzer
        run: |
          mkdir -p priv/plts
          mix dialyzer --format short

  docker-e2e:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Cache deps
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile
        run: mix compile

      - name: Pre-pull test images in parallel
        run: |
          for img in alpine:3.19 alpine:latest ubuntu:24.04 postgres:15 postgres:16 redis:7 redis:7-alpine; do
            docker pull "$img" &
          done
          wait

      - name: Run docker E2E tests
        env:
          LOOPYARD_LONG_TIMEOUTS: "1"
          LOOPYARD_DOCKER_TESTS: "1"
        run: mix test --only docker --exclude macos --exclude ssh --exclude terminal
        timeout-minutes: 15
