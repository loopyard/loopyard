---
project: garryslist
started_at: 2026-03-25T22:58:00Z
finished_at: 2026-03-26T07:50:00Z
result: failed
agent_id: bbf2cb18947d34af
tool_calls: 441
errors: 5
checklist_completed: 8/11
prompt_version: setup_guide_v1
---

# Eval: garryslist — 2026-03-25

## Summary

Setup agent got most of the way — read project files, wrote Dockerfile, added postgres+redis, installed gems and npm packages, set up database. Got stuck in a rebuild loop when containers wouldn't come back up.

## What worked

- Read project files correctly (Gemfile, Procfile.dev, package.json, database.yml, Dockerfile)
- Identified Ruby 3.4, PostgreSQL, Redis as dependencies
- Wrote a dev Dockerfile with ruby:3.4-slim base
- Used `BUNDLE_PATH=/usr/local/bundle` (correct for bind mount)
- Added postgres and redis services
- Installed gems and npm packages via `exec_stream`
- Detected PostGIS requirement from db/structure.sql and swapped postgres image
- Added `pg_isready` wait loop to dev command

## What failed

### 1. Rebuild loop (critical)
Agent called `rebuild` repeatedly without checking why the build was failing. Kept seeing old exited containers and retrying instead of reading build output. The streaming rebuild wasn't showing errors to the agent (it was there but agent didn't process it).

**Root cause:** Agent doesn't check the `:build_failed` message content after rebuild. It just calls `service_status` and sees containers down, then rebuilds again.

**Fix needed:** Prompt should say: "After rebuild, if services are still down, read the Rebuild output message above — it contains the build log with the error."

### 2. Reading its own agent log
Agent tried to read `.boomlooper/workspace/agents.log` looking for Docker build logs. This is the wrong file — it's the agent persistence log, not Docker output.

**Fix needed:** Agent should never read files in `.boomlooper/workspace/` — that's BoomLooper internal state.

### 3. Stream timeout
Agent stopped responding twice (hit the 2-minute timeout). Recovered when user sent "Keep going".

**Possible cause:** Long-running tool calls (bundle install, npm install) may have exceeded the stream timeout. The timeout should probably be longer, or reset on each tool call.

## Prompt changes made during this session

1. Moved setup guide from system prompt to `priv/prompts/setup_guide.md` (system prompt was 8.7K chars, CLI was getting SIGKILL'd)
2. Added `cli_path: :global` to use system claude instead of SDK bundled version (bundled v2.1.76 was crashing)
3. Slimmed system prompt to ~765 chars with 2000 char test limit

## Prompt changes needed

1. Add to setup guide: "After rebuild, if services show as stopped, READ the build output in the chat above. Don't just retry."
2. Add to setup guide: "Never read files in .boomlooper/workspace/ — that's internal state."
3. Consider increasing stream timeout from 2 minutes to 5 minutes for setup agents (they run long tool calls)
