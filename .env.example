# Boom Looper Environment Configuration
# Copy to .env and customize. Values here are loaded automatically in dev/test.

# HTTP Basic Auth (optional — leave blank to disable)
# BOOM_LOOPER_AUTH_PASSWORD=changeme
# BOOM_LOOPER_AUTH_USERNAME=admin

# Production only:
# SECRET_KEY_BASE=generate-with-mix-phx-gen-secret
# PHX_HOST=boom-looper.example.com
# PORT=4000
