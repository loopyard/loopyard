{
  "services": {
    "dev": {
      "build": {
        "context": "/Users/bradgessler/Projects/bradgessler/hive/.hive",
        "dockerfile": "Dockerfile"
      },
      "command": "mix phx.server",
      "environment": [
        "MIX_ENV=dev",
        "PHX_HOST=localhost",
        "PORT=4000"
      ],
      "ports": [
        "4000:4000"
      ],
      "volumes": [
        "/Users/bradgessler/Projects/bradgessler/hive:/workspace",
        "cache-40ee:/root/.cache"
      ],
      "working_dir": "/workspace"
    },
    "workspace": {
      "build": {
        "context": "/Users/bradgessler/Projects/bradgessler/hive/.hive",
        "dockerfile": "Dockerfile"
      },
      "command": "sleep infinity",
      "environment": [
        "MIX_ENV=dev",
        "PHX_HOST=localhost",
        "PORT=4000"
      ],
      "volumes": [
        "/Users/bradgessler/Projects/bradgessler/hive:/workspace",
        "cache-40ee:/root/.cache"
      ],
      "working_dir": "/workspace"
    }
  },
  "volumes": {
    "cache-40ee": null
  }
}